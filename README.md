# 🧩 Лабораторна робота №4
## **Автоматизована перевірка норм кодування програмного коду для підтримки безперервної інтеграції ПЗ з використанням GitHub Actions**


### 🎯 **Мета роботи**
Ознайомитися з механізмами безперервної інтеграції (CI) на платформі **GitHub Actions** та навчитися автоматично перевіряти якість і відповідність коду стандартам для різних типів файлів (JSON, SQL, Python).


### ⚙️ **Опис проєкту**
Репозиторій містить приклади **автоматичних перевірок (workflow)**, які виконуються під час кожного `push` або `pull request` у гілку `main`.
Для різних типів файлів реалізовано окремі CI-процеси:

| Workflow | Призначення | Інструменти |
|-----------|--------------|-------------|
| **Data Schema Code Convention** | Перевірка форматування JSON- та SQL-схем | `jsonlint`, `sqlfluff` |
| **Program Code Convention Check** | Аналіз Python-коду на помилки стилю | `flake8` |
| **Print Software Versions** | Виведення версій середовищ і баз даних | `python3`, `node`, `psql`, `mongo` |

### 🧠 **Структура репозиторію**
| Файл / Папка | Призначення |
|---------------|-------------|
| `DataSchema.json` | Початкова JSON-схема бази даних |
| `DataSchema.sql` | Початкова SQL-схема бази даних |
| `DataSchemaModified.json` | Виправлена та перевірена JSON-схема |
| `DataSchemaModified.sql` | Виправлена SQL-схема, що пройшла лінтинг |
| `ProgramCode.py` | Основний програмний код |
| `ProgramCodeModified.py` | Виправлений Python-код із урахуванням стандартів |
| `PrintSoftwareVersions.yml` | Workflow для перевірки версій середовищ |
| `DataSchemaCodeConvention.yml` | CI для JSON/SQL лінтингу |
| `ProgramCodeConvention.yml` | CI для Python-коду |
| `*.jpg` | Скриншоти результатів перевірок у GitHub Actions |


### 🚀 **Як це працює**
1. При кожному `push` або `pull request` запускаються GitHub Actions.
2. Кожен workflow виконує автоматичну перевірку файлів:
   - JSONLint перевіряє правильність форматування JSON;
   - SQLFluff аналізує SQL-схеми за стандартом PostgreSQL;
   - flake8 перевіряє Python-код на стиль PEP8.
3. У разі помилки — повідомлення з’являється у вкладці **Actions** у GitHub.


### 🧾 **Результати**
- Автоматизовано перевірку стилю JSON, SQL та Python файлів.
- Виявлені й виправлені помилки форматування.
- Налаштовано CI-процеси, які виконуються автоматично при оновленні коду.


### 👨‍💻 **Автор**
** Громадченко Єгор [Egor781-cyber]**
Студент групи **AI-233**,
Одеський політехнічний університет

📧 *egor23255@stud.op.edu.ua*

